# Persistence settings.
# These are currently read by the API crate and passed into the persistence crate.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/mock_interview_ai
DB_MAX_CONNECTIONS=10

# Domain crate settings.
# The domain crate does not read environment variables directly.
# These defaults are read by the API crate and stored in domain snapshots.

SESSION_TTL_HOURS=72
DEFAULT_PASS_THRESHOLD=70
